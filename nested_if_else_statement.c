#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
//now start the nested if-else statement don't waste the time let's start the code!!!
void main(){
    int a,b;
    printf("Enter the 1st value :");
    scanf("%d", &a);
    printf("Enter the 2nd value :");
    scanf("%d", &b);
    // start the nested if statement block
    if (a ==20) // 1st if block stared 
    {
        if (b ==30){   // 2nd inner if block 
            printf("A is the 20, and B is teh 30 it's true value's hy hy hy.."); // print block 
        }
    }
        else{
            printf("A and B value's are not matched"); // else block
        }
}

// today having very fun with C lang code awh awh awh awh......................

