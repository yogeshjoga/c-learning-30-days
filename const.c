#include<stdio.h>
#include<math.h>
#include<stdlib.h>
#include<string.h>

int main(){ //main function for declaration
    const int side=20;  // const variable --like to writen as a     int const side = 19; or const side = 10; also we will writen a code
    int area;     //allowcate the variable is 
    area = side*side;  // area is a variable to multplycation is side* side
    printf("total area of square is : %d, %d", side, area);   //printf is the predefine function to use a out of the print values and call the functions 
}̥