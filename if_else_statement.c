#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
// if and else statement functions by using this code 

int main(){
       int a,b,c;
       a =10;
       b =12;
      // c =15;
       if(a > b) {
           printf("A is the small in B");
       }
       else {
           printf("B is the bigger the A");
       }
     
}
